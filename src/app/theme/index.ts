import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#f9f9f9',
      main: '#161616',
      dark: '#686868'
    },
    secondary: {
      main: '#ffd710'
    }
  }
});

export default theme;
